<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security-4.0.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-3.0.xsd">

    <context:component-scan base-package="com.ghome.media" />

    <!-- SP (Services Provider) -->
    <security:http name="spFilterChain" entry-point-ref="entryPoint" use-expressions="true">

        <!-- Disable cross domain security -->
        <security:csrf disabled="true"/>

        <!-- Authentication -->
        <security:custom-filter before="PRE_AUTH_FILTER" ref="spTokenUnmarshallingFilter"/>
        <security:custom-filter position="PRE_AUTH_FILTER" ref="spAuthenticationFilter"/>

        <!-- Security access settings -->
        <security:intercept-url pattern="/login.jsp*" access="isAnonymous()"/>
        <security:intercept-url pattern="/index.html" access="hasRole('ROLE_USER')"/>
    </security:http>



    <security:authentication-manager alias="idpAuthenticationManager">
        <security:authentication-provider>
            <security:user-service>
                <security:user name="admin" password="admin" authorities="ROLE_USER" />
            </security:user-service>
        </security:authentication-provider>
        <security:authentication-provider ref="spAuthenticationManager"/>
    </security:authentication-manager>

</beans>